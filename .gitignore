# Node / build
node_modules/
dist/
coverage/
.vite/
# NOTE: `build/` sengaja TIDAK diignore di sini — direktori itu berisi skrip
# build sumber yang di-commit (build/prepare-sites-output.js, sites-vite-plugin.js,
# bundle-size-report.js), bukan output build (yang keluar ke `dist/` di atas).
# (US10 spec 002, T057 — ditemukan saat build/bundle-size-baseline.json baru
# ternyata tidak akan ter-track oleh pola lama.)

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs & temp
*.log
tmp/
temp/
.wrangler/

# Env / secrets
.env
.env.*
!.env.example

.wrangler/
.lighthouseci/
docs/sample/brilliant/
docs/sample/mimo/
docs/sample/pathway/